# Changelog

Morphix does not publish a detailed changelog on this site.

- The detailed change history lives **inside the repository**: the git history and the internal engineering log (`AGENTS.md` in the project root).
- **Release notes** are published together with each release.

---

Morphix no publica un changelog detallado en este sitio.

- El historial detallado de cambios vive **dentro del repositorio**: la historia de git y el log de ingeniería interno (`AGENTS.md` en la raíz del proyecto).
- Las **notas de versión** se publican junto con cada release.
