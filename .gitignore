# ── Runtime artifacts (created by the application) ──
logs/
/memory/
/exports/
/sessions/
/workspaces/

# ── Secrets & credentials ──
.env
*.db

# ── Logs ──
*.log

# ── Python artifacts ──
__pycache__/
*.py[cod]
*.egg-info/
dist/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
.coverage.*
data/workspaces

# ── Virtual environments ──
venv/
.venv/

# ── OS artifacts ──
.DS_Store

# ── Test artifacts ──
*.png

# ── Docs build ──
site/

# ── Internal dev rules → see .git/info/exclude (cp dev/.gitignore.exclude .git/info/exclude) ──
coverage.json
